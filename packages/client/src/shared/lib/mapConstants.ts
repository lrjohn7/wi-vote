/** Default map center biased toward Milwaukee metro (population center). */
export const WISCONSIN_CENTER: [number, number] = [-87.95, 43.04];

/** Geographic center of Wisconsin (used for statewide views). */
export const WISCONSIN_GEO_CENTER: [number, number] = [-89.5, 44.5];

/** Bounding box for Wisconsin to prevent panning away. */
export const WISCONSIN_BOUNDS: [[number, number], [number, number]] = [
  [-93.0, 42.3],
  [-86.7, 47.2],
];
